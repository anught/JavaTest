序列化 ：把对象转换为字节序列的过程
反序列化：把字节序列恢复为对象的过程

为什么要序列化：
1、持久存储
2、网络传输时
3、通过RMI传输对象

实现方式及注意事项
transient static 不会序列化
当属性时对象时，也要实现序列化接口

serialVersionUID要赋值 ？？
 
如果实现了接口后；如果不设定UID，会计算自动生成一个UID；；当类中的属性发生变化时
反序列化过程中会重新计算UID，此时的UID发生了变化 ，从而会导致反序列化失败

